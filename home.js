function setup() {
  createCanvas(windowWidth, windowHeight);
	angleMode(DEGREES);
	background(210, 20, 20);

  // button10 = createButton("10");
  // button10.position(windowWidth/2 - 600, windowHeight/2);
  // button10.style("height", "100");
  // button10.mousePressed(loop);
  //
  // button15 = createButton("15");
  // button15.position(windowWidth/2 - 300, windowHeight/2);
  //
  // button20 = createButton("20");
  // button20.position(windowWidth/2, windowHeight/2);
  //
  // button30 = createButton("30");
  // button30.position(windowWidth/2 + 300, windowHeight/2);
  //
  // button60 = createButton("60");
  // button60.position(windowWidth/2 + 600, windowHeight/2);

  // noLoop();
}


function draw() {

  timer45();

  // noStroke();
  // fill(0, 0, 0);
  // ellipse(windowWidth/2 - 600, windowHeight/2,  windowHeight/6, windowHeight/6);
  //
  // noStroke();
  // fill(0, 0, 0);
  // ellipse(windowWidth/2 - 300, windowHeight/2,  windowHeight/6, windowHeight/6);
  //
  // noStroke();
  // fill(0, 0, 0);
  // ellipse(windowWidth/2, windowHeight/2,  windowHeight/6, windowHeight/6);
  //
  // noStroke();
  // fill(0, 0, 0);
  // ellipse(windowWidth/2 + 300, windowHeight/2,  windowHeight/6, windowHeight/6);
  //
  // noStroke();
  // fill(0, 0, 0);
  // ellipse(windowWidth/2 + 600, windowHeight/2,  windowHeight/6, windowHeight/6);



}
